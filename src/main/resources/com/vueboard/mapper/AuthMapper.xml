<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vueboard.domains.auth.mapper.AuthMapper">

	<!-- 로그인 쿼리 -->
	<select id="getUserInfoById"
		resultType="com.vueboard.domains.auth.dto.UserResponseDTO">
		SELECT
		PN,
		MEMBER_ID AS memberId,
		PASSWORD,
		NAME,
		EMAIL,
		REGDATE
		FROM TB_MEMBER
		WHERE MEMBER_ID = #{memberId}
	</select>

	<!-- 해당 pn값에 해당하는 user 정보 조회 -->
	<select id="findByPn"
		resultType="com.vueboard.domains.auth.dto.UserResponseDTO">
		SELECT
		PN,
		MEMBER_ID AS memberId,
		PASSWORD,
		NAME,
		EMAIL,
		REGDATE
		FROM TB_MEMBER
		WHERE PN = #{pn}
	</select>
</mapper>