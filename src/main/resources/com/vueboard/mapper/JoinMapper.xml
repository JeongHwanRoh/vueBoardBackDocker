<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vueboard.domains.auth.mapper.JoinMapper">

	<select id="checkEmailDuplicate" parameterType="String"
		resultType="int">
		SELECT COUNT(email)
		FROM TB_MEMBER
		WHERE email = #{email}
	</select>

	<insert id="addUser"
		parameterType="com.vueboard.domains.auth.entity.User">
		INSERT INTO TB_MEMBER(
		member_id,
		email,
		password,
		name
		)
		VALUES(
		#{memberId},
		#{email},
		#{password},
		#{name}
		)
	</insert>

</mapper>