<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vueboard.domains.kanban.mapper.KanbanCardMapper">
	<select id="findByBoardId" parameterType="String"
		resultType="com.vueboard.domains.kanban.entity.KanbanCard">
		SELECT
			CARD_ID AS cardId,
			BOARD_ID AS boardId,
			TITLE AS title,
			CARD_INFO AS cardInfo,
			STATUS AS status,
			ORDER_NUM AS orderNum,
			CREATED_AT AS createdAt,
			UPDATED_AT AS updatedAt
		FROM TB_KANBAN_CARD
		WHERE BOARD_ID = #{boardId}
		ORDER BY ORDER_NUM ASC, CARD_ID ASC
	</select>
</mapper>